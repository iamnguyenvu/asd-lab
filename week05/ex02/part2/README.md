# Part 2 notes

Each `baiXX` folder contains a standalone `docker-compose.yml`.
Run from inside each folder:

```bash
docker compose config
docker compose up -d --build
docker compose ps
docker compose down -v
```

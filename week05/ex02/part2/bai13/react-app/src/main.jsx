import React from "react";
import ReactDOM from "react-dom/client";

function App() {
  return <h1>React served by Nginx (bai13)</h1>;
}

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
